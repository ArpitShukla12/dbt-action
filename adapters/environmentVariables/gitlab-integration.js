import dotenv from "dotenv";

dotenv.config();

const {
  IS_DEV,
  ATLAN_INSTANCE_URL,
  CI_PROJECT_PATH,
  CI_MERGE_REQUEST_IID,
  CI_PROJECT_ID,
  CI_JOB_URL,
  ATLAN_API_TOKEN,
  IS_IGNORE_MODEL_ALIAS_MATCHING,
  GITLAB_TOKEN,
  CI_COMMIT_MESSAGE,
  GITLAB_USER_LOGIN,
  CI_PROJECT_NAME,
} = process.env;

export {
  IS_DEV,
  ATLAN_INSTANCE_URL,
  CI_PROJECT_PATH,
  CI_MERGE_REQUEST_IID,
  CI_PROJECT_ID,
  CI_JOB_URL,
  ATLAN_API_TOKEN,
  IS_IGNORE_MODEL_ALIAS_MATCHING,
  GITLAB_TOKEN,
  CI_COMMIT_MESSAGE,
  GITLAB_USER_LOGIN,
  CI_PROJECT_NAME,
};
